// JavaScript tidak diperlukan untuk fungsionalitas dasar scroll horizontal
// Namun, kita bisa menambahkan kode untuk navigasi tambahan jika diperlukan

document.addEventListener('DOMContentLoaded', function() {
    // Opsional: Tambahkan indikator scroll jika diinginkan
    const sliderWrapper = document.querySelector('.slider-wrapper');
    
    // Mendeteksi perubahan scroll untuk mengaktifkan indikator (jika diimplementasikan)
    sliderWrapper.addEventListener('scroll', function() {
      // Fungsionalitas tambahan dapat ditambahkan di sini
    });
  });